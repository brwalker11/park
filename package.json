{
  "name": "monetize-parking",
  "version": "1.0.0",
  "private": true,
  "description": "Static marketing website for Monetize Parking",
  "scripts": {
    "generate:articles": "node tools/generate-article-pages.js",
    "generate:sitemap": "node tools/update-sitemap.js",
    "generate:thumbnails": "node tools/generate-thumbnails.js",
    "build": "npm run generate:thumbnails && npm run generate:articles && npm run generate:sitemap"
  },
  "devDependencies": {
    "cheerio": "^1.1.2",
    "cssnano": "^7.1.2",
    "postcss": "^8.5.6",
    "postcss-cli": "^11.0.1",
    "sharp": "^0.33.5",
    "svgo": "^4.0.0",
    "terser": "^5.46.0",
    "xlsx": "^0.18.5"
  }
}
